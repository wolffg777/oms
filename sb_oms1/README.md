# Order Management System

Internal operations tool for managing the order pipeline.

## Structure

- `/backend` — Node.js + Express + Prisma API
- `/frontend` — React + TypeScript client

## Setup

See README files in each subdirectory.

## Status

🚧 In development
